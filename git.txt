Proyecto existente pasos:

    1.- $ git init 
    2.- $ git add . 
    3.- $ git commit -m "First commit" 
    4.- $ git remote add origin remote repository URL 
    5.- $ git remote -v 
    6.- $ git push origin master
